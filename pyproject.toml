[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "gt7tracetelem"
version = "0.1.6"
description = "TRACE - GT7 telemetry capture, live dashboard, and lap/race analysis toolkit"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [
    {name = "Ransh"},
]
keywords = [
    "gran-turismo",
    "gt7",
    "telemetry",
    "sim-racing",
    "racing",
    "playstation",
    "motorsport",
    "dashboard",
    "lap-analysis",
    "udp",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: End Users/Desktop",
    "Topic :: Games/Entertainment :: Simulation",
    "Topic :: Multimedia :: Graphics",
    "Topic :: Scientific/Engineering :: Visualization",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Environment :: Win32 (MS Windows)",
    "Environment :: MacOS X",
    "Environment :: X11 Applications",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Natural Language :: English",
]
dependencies = [
    "numpy",
    "pandas",
    "matplotlib",
    "pycryptodome",
]

[project.urls]
Homepage = "https://gt7trace.netlify.app/"
Repository = "https://github.com/ransh2014/gt7telemtrace"
Issues = "https://github.com/ransh2014/gt7telemtrace/issues"
Changelog = "https://github.com/ransh2014/gt7telemtrace/blob/main/CHANGELOG.md"

[project.scripts]
gt7telem = "gt7telem.launcher:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
gt7telem = ["*.csv", "py.typed"]

[project.optional-dependencies]
dev = [
    "pytest",
    "ruff",
]

[tool.ruff]
line-length = 120
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
# E501 line-length, E701/E702 multi-statement lines, E722 bare-except, and
# E731 lambda-assignment are all deliberate, pervasive conventions in the
# GUI/plotting modules (dense matplotlib one-liners, defensive catch-alls
# around live packet parsing, local byte-unpack lambdas) -- not bugs.
ignore = ["E501", "E701", "E702", "E722", "E731"]
